services:
  - type: web
    name: sentry-neural-pentesting-brain
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements-minimal.txt && mkdir -p backend/static && echo "Backend-only build complete"
    startCommand: cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./sentry.db
      - key: SECRET_KEY
        generateValue: true
      - key: ENVIRONMENT
        value: production
      - key: CORS_ORIGINS
        value: "*"
      - key: OWNER_EMAILS
        value: "saifullahpathan49@gmail.com,saifullah.pathan24@sanjivani.edu.in"
    disk:
      name: sentry-disk
      mountPath: /opt/render/project/backend
      sizeGB: 1